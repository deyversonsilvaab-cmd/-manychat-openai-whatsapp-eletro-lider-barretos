import OpenAI from"openai";import fs from"fs";import path from"path";import{type}from"./attachment.js";
const client=new OpenAI({apiKey:process.env.OPENAI_API_KEY});const MODEL=process.env.VISION_MODEL||process.env.OPENAI_MODEL||"gpt-5.5";
const schema={type:"object",additionalProperties:false,required:["transcription","items","confidence","needsBetterImage"],properties:{transcription:{type:"string"},confidence:{type:"string",enum:["alta","media","baixa"]},needsBetterImage:{type:"boolean"},items:{type:"array",items:{type:"object",additionalProperties:false,required:["descricao","quantidade","observacao"],properties:{descricao:{type:"string"},quantidade:{type:"string"},observacao:{type:"string"}}}}}};
export async function readFile({url,message=""}){try{let prompt=fs.readFileSync(path.join(process.cwd(),"prompts","ocr.txt"),"utf8")+`\nMensagem do cliente: ${message}\nURL: ${url}\nTipo: ${type(url)}`;let content=[{type:"text",text:prompt}];if(type(url)!=="pdf")content.push({type:"image_url",image_url:{url}});else content.push({type:"text",text:"Se o PDF não puder ser lido pelo modelo, peça print/foto nítida."});let r=await client.chat.completions.create({model:MODEL,temperature:0,messages:[{role:"user",content}],response_format:{type:"json_schema",json_schema:{name:"ocr_orcamento",strict:true,schema}}});return JSON.parse(r.choices?.[0]?.message?.content||"{}")}catch(e){console.error("vision error",e?.message||e);return{transcription:"",items:[],confidence:"baixa",needsBetterImage:true,error:"Falha ao interpretar arquivo."}}}
